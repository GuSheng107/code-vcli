# vcli VLM 依赖（Qwen2.5-VL 视觉语言模型，GPU/CUDA 场景）
# torch/torchvision 由 init 阶段根据计算模式单独安装，不在此声明
transformers==4.51.3
qwen-vl-utils>=0.0.8
accelerate>=1.0.0
# AWQ 运行时由 TypeScript 安装器按平台单独安装；BF16 不安装 AWQ 依赖。